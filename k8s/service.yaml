apiVersion: v1
kind: Service
metadata:
  name: sky-poc-backend
  namespace: staging
spec:
  selector:
    app: sky-poc-backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP
